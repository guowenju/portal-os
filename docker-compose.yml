services:
  portal-os:
    image: guowenju/portal-os:latest
    container_name: portal-os
    restart: unless-stopped
    environment:
      RUST_LOG: info
    ports:
      - '9090:9090'
    volumes:
      - ./apps/docs:/app/content/docs
